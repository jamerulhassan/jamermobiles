RewriteEngine On
RewriteBase /

# If request is an existing file or folder, serve it directly
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, always serve index.html
RewriteRule ^ index.html [L]
